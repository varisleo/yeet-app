import 'reflect-metadata';

// This is a minimal setup file for all tests
// Integration tests have their own setup in integration/setup.ts

// Increase timeout for potential container startup in integration tests
jest.setTimeout(60000);
